<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.MainPage"
             Title="Notatki">
    <StackLayout Margin="40">
        <Label Text="Dodaj notatkę" FontSize="Large" Margin="0,0,0,20"/>
        <Entry x:Name="title" Placeholder="Tytuł..." FontSize="Large"/>
        <Editor x:Name="note" Placeholder="Notatka..." HeightRequest="150"/>
        <Button Text="Dodaj" Clicked="SubmitClicked"/>
        <ListView x:Name="notesList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <FlexLayout AlignItems="Center">
                            <Label Text="{Binding Title}" HorizontalTextAlignment="Center" BindingContext="{Binding .}"/>
                            <Button Text="Usuń" Clicked="DeleteClicked"/>
                        </FlexLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
